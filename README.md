# 🤖 AI Accessibility QA Agent

An autonomous AI agent designed to bridge the gap between automated accessibility scanning and actionable developer feedback. It observes WCAG violations, reasons about their real-world impact using LLMs, and acts by creating detailed GitHub issues.

## 🚀 Overview

Traditional accessibility scanners (like axe-core) tell you **what** is broken. This agent tells you **who** is affected, **why** it matters for the business, and **how** to fix it—all while autonomously managing your GitHub issue tracker.

### The Loop: Observe → Reason → Act

1.  **Observe**: Uses Playwright and axe-core to perform a deep scan of the target URL for WCAG 2.0/2.1 violations.
2.  **Reason**: Sends structured violation data to the **MiniMax-M2** model to analyze user impact, business risk, and remediation steps.
3.  **Decide**: Applies a confidence-weighted logic to determine the next step:
    *   **Confidence ≥ 80%**: Auto-escalates (Creates a GitHub Issue).
    *   **Confidence 50-80%**: Flags for human review.
    *   **Confidence < 50%**: Logs for audit only.
4.  **Act**: For high-confidence issues, it automatically populates your GitHub repository with descriptive, formatted issues including remediation code snippets.

---

## ✨ Features

*   **Autonomous Triage**: No more manually sorting through hundreds of "moderate" violations.
*   **User-Centric Analysis**: Identifies exactly which user groups (screen-reader users, keyboard-only users, etc.) are blocked.
*   **Remediation Hints**: Provides "Before" (Broken) and "After" (Fixed) HTML snippets.
*   **Premium CLI UI**: A beautiful terminal interface inspired by modern AI agents, featuring phase headers, live spinners, and summary cards.
*   **Confidence Thresholds**: Ensures high-quality automation while flagging uncertain cases for manual verification.

---

## 🛠️ Setup

### Prerequisites

*   **Node.js**: v18 or higher.
*   **Git**: For repository management.
*   **GitHub Token**: A Personal Access Token (PAT) with `repo` scope.
*   **MiniMax API Key**: From [platform.minimax.io](https://platform.minimax.io).

### Installation

1.  **Clone and Install**:
    ```bash
    git clone <your-repo-url>
    cd qa-agent
    npm install
    npx playwright install chromium
    ```

2.  **Configure Environment**:
    Create a `.env` file in the root directory:
    ```env
    MINIMAX_API_KEY=your_minimax_api_key
    GITHUB_TOKEN=your_github_personal_access_token
    GITHUB_REPO=username/repository-name
    ```

---

## 📖 Usage

### Run an Accessibility Audit
Scan any URL and let the agent triage and file issues:
```bash
node agent.js audit https://example.com
```

### Explain a Violation
Get a plain-English explanation of a specific WCAG rule:
```bash
node agent.js explain color-contrast
```

---

## 📂 Project Structure

```
qa-agent/
├── agent.js          # Main CLI entry point and agent logic
├── ui.js             # Terminal UI components and styling
├── tools/
│   ├── scanner.js    # Playwright + axe-core integration
│   └── github.js     # GitHub REST API client
├── .env              # Local environment variables
└── package.json      # Dependencies and scripts
```

---

## 🛡️ License

Distributed under the MIT License. See `LICENSE` for more information.

---

*Generated by the AI Accessibility QA Agent.*
